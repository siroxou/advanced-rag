# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# --- Node / Next.js ---
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo

# --- Env / secrets ---
.env
.env.*
!.env.example

# --- Data / models / artifacts (never commit corpora or weights) ---
data/
models/
adapters/
*.gguf
*.safetensors
*.bin
ml/datasets/*.jsonl
# ...except the curated golden eval set, which is committed so the CI gate is deterministic.
!ml/datasets/golden.jsonl
# The eval gate's summary artifact (backend/metrics.json by default).
metrics.json

# --- OS / editor / local tooling ---
.DS_Store
.idea/
.vscode/
.claude/

# --- Logs ---
*.log

# --- Build artifacts ---
dist/
